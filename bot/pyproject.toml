[project]
name = "lms-telegram-bot"
version = "0.1.0"
description = "Telegram bot for LMS backend"
requires-python = ">=3.12"
dependencies = [
    "aiogram>=3.0",
    "httpx>=0.27",
    "openai>=1.0",
]
